<script type="text/x-handlebars" data-template-name="index">
    <ul>
      {{#each songs as |song|}}
          <li>{{song.title}} by {{song.band}} ({{song.rating}})
          </li>
      {{/each}}
    </ul>
</script>

{{outlet}}